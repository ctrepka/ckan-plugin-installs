# ckan-plugin-installs

## Overview
This file executes basic bash commands to intall plugins for ckan.
Used with the keitaroinc ckan docker image, but use of install variables intended to allow for some portability.
